# 班级管理系统依赖 / Classroom Manager Application Dependencies
# 
# 安装依赖: pip install -r requirements.txt
# Install: pip install -r requirements.txt

# 核心库 / Core libraries
openpyxl>=3.10.0              # Excel文件支持 / Excel file support
matplotlib>=3.5.0              # 图表绘制 / Chart plotting
PyInstaller>=5.0               # 打包为可执行文件 / Package as executable

# 可选库 / Optional libraries
# 如需要额外功能，可取消注释以下行
# Uncomment the following lines if additional features are needed
# pandas>=1.3.0                # 数据处理 / Data processing
# scipy>=1.7.0                 # 科学计算 / Scientific computing
